[package]
name = "substrate-inference-bin"
description = "Inference daemon binary: thin entry point that loads substrate.toml and starts the inference service"
version.workspace = true
edition.workspace = true

[[bin]]
name = "substrate-inference"
path = "src/main.rs"

[dependencies]
substrate-inference = { workspace = true }
tokio              = { workspace = true }
anyhow             = { workspace = true }
tracing            = { workspace = true }
tracing-subscriber = { workspace = true }
