# Contract: agent-management

## Parties
ccd  <->  cloud-code (Claude Code) agent processes

## What the edge carries
Spawn / track / route / signal cloud-code agent processes — the multi-agent
substrate CCD owns ("run all cloud code through the Marshall"). Novel edge; the
foundation Org's inter-agent communication is layered on. Shaped-for note: Org will
want to observe and address agents by handle through this surface. Schema deferred.
